version: 3
projects:
  - name: terraform-project
    dir: .
    workflow: windows-workflow  # Yahan custom workflow define kar rahe hain
    autoplan:
      when_modified: ["*.tf", "*.tfvars"]
      enabled: true

